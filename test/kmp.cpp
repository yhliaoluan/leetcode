#include "../src/header.h"

static vector<int> get_next(string s) {
    vector<int> next(s.size(), 0);
    return next;
}

int main(int argc, char **argv) {
    print_vector(argv[1]);
    return 0;
}
